---
# Proxy 服务器组专用配置
# 适用于 proxy_servers 组的所有服务器

# ====================
# Proxy 服务器额外公开端口
# ====================
# 这些端口会添加到防火墙公开端口列表中
proxy_additional_ports:
  - 11111  # Proxy 服务端口
  - 8443    # 备用 HTTPS 端口
  - 22222  # 备用 Proxy 端口
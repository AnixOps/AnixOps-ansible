---
# Minimal inventory example for the AnixOps self-hosted web stack.

all:
  children:
    anixops_selfhosted_servers:
      hosts:
        # anixops-web-1:
        #   ansible_host: "{{ lookup('env', 'ANIXOPS_WEB_1_V4_SSH') }}"
      vars:
        server_role: selfhosted_web

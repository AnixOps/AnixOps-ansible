---
# =============================================================================
# V2board Handlers | V2board 处理器
# =============================================================================

# Docker restart (used when deploy_method == docker)
- name: Restart v2board service | 重启 v2board 服务
  ansible.builtin.command:
    cmd: docker restart {{ v2board_docker_container_name }}
  failed_when: false
